<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <!-- Columns are set/refreshed by post-migrate (registrar directory field list). -->
        <record id="report_student_directory" model="kai.view.report">
            <field name="name">Student directory</field>
            <field name="description">Registrar export matching the student directory column list.</field>
            <field name="model_id" ref="model_school_student"/>
            <field name="domain">[]</field>
            <field name="is_shared" eval="True"/>
            <field name="sequence">5</field>
            <field name="view_mode">list,form</field>
        </record>

    </data>
</odoo>
