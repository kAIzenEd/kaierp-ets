<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- noupdate=1: first install only. Never overwrite production settings on upgrade.
         Admission webhook secret is NOT seeded here — set it once in
         kAI-ERP → Settings (or Technical → System Parameters:
         kaierp.admission_webhook_secret) to match the website ODOO_WEBHOOK_SECRET. -->
    <data noupdate="1">

        <!-- Allow API key generation for integration users -->
        <record id="base_enable_programmatic_api_keys" model="ir.config_parameter">
            <field name="key">base.enable_programmatic_api_keys</field>
            <field name="value">True</field>
        </record>

        <!-- Public website base used in referee emails (online reference forms) -->
        <record id="kaierp_public_admissions_base_url" model="ir.config_parameter">
            <field name="key">kaierp.public_admissions_base_url</field>
            <field name="value">https://apply.ets-india.org</field>
        </record>

        <!-- Dedicated user for website → Odoo integration -->
        <record id="partner_ets_website_api" model="res.partner">
            <field name="name">ETS Website Integration</field>
            <field name="email">ets.website.api@ets-aca.org</field>
        </record>

        <record id="user_ets_website_api" model="res.users">
            <field name="name">ETS Website Integration</field>
            <field name="login">ets.website.api@ets-aca.org</field>
            <field name="partner_id" ref="partner_ets_website_api"/>
            <field name="group_ids" eval="[
                (4, ref('base.group_user')),
                (4, ref('kaierp.group_school_manager')),
            ]"/>
        </record>

    </data>
</odoo>
